import Ember from 'ember';

export default Ember.View.extend({
  setAllTheHeights: function() {
    //setTimeout( function(){
      // console.log('running code now!');
      // var t=0; // the height of the highest element (after the function runs)
      // var t_elem;  // the highest element (after the function runs)
      // Ember.$("*",Ember.$('body>.ember-view')).each(function () {
      //   Ember.this = Ember.$(this);
      //   if ( Ember.$(this).outerHeight() > t ) {
      //     t_elem=this;
      //     console.log(t_elem);
      //     t=Ember.$(this).outerHeight();
      //   }
      // });

      // var windowHeight = Ember.$(window).height();
      // var documentHeight = t;

      // if (windowHeight < documentHeight) {
      //   Ember.$('body, .background-normal, .background-jazzy, .background-jazzy-2').height(documentHeight);
      // } else  {
      //   Ember.$('body').height('100%');
      //   Ember.$('.background-normal, .background-jazzy, .background-jazzy-2').height(documentHeight);
      // }
    //}, 100);
  }
});
