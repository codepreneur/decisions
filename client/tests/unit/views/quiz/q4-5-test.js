import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q4-5', 'QuizQ45View');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
