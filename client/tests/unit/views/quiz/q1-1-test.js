import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q1-1', 'QuizQ11View');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
