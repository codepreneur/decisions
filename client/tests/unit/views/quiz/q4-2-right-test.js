import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q4-2-right', 'QuizQ42RightView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
