import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q4-7-right', 'QuizQ47RightView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
