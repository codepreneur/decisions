import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/result2', 'QuizResult2View');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
