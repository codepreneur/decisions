import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q2-down', 'QuizQ2DownView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
