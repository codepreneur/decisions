import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q4-4-right', 'QuizQ44RightView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
