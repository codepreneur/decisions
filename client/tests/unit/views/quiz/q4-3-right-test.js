import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q4-3-right', 'QuizQ43RightView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
