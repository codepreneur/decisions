import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quiz/q3-1', 'QuizQ31View');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
